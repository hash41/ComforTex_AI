/// Raw method, which would be only available for web.
void redirectToAdminBoard() {
  // This is a stub implementation for mobile/desktop.
  // It can be empty or throw an error if this functionality isn't supported
  // there.
  throw UnsupportedError('Admins should use web browsers to login');
}
